{
  "kind": "collectionType",
  "collectionName": "follows",
  "info": {
    "name": "follow"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "dj": {
      "model": "dj",
      "via": "follows"
    },
    "user": {
      "plugin": "users-permissions",
      "model": "user",
      "via": "follows"
    }
  }
}
